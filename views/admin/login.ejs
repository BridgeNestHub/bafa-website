<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Login | Melba Community Center</title>
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    /* Full-page flexbox for perfect vertical and horizontal centering */
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background-color: #f8f9fa; /* A slightly softer gray */
    }
    #password-toggle {
      border-top-right-radius: 0;
      border-bottom-right-radius: 0;
      background-color: #f1f3f5;
      border-color: #dee2e6;
    }
    .login-container {
      max-width: 400px;
      width: 100%; /* Ensures it fills the max-width */
      padding: 1rem; /* Add padding for smaller screens */
    }
    
    .login-logo {
      text-align: center;
      margin: 0 0 2rem 0;
    }
    
    .login-logo img {
      width: auto;
      transition: transform 0.3s ease; /* Adds a smooth transition for the hover effect */
    }

    .login-logo img:hover {
      transform: scale(1.05); /* Logo slightly grows on hover for an interactive feel */
    }

    .login-card {
      border: none;
      border-radius: 12px; /* Slightly more modern rounded corners */
      box-shadow: 0 8px 25px rgba(0,0,0,0.1); /* Softer, more pronounced shadow */
      overflow: hidden; /* Ensures header corners are contained */
    }
    
    /* Using a subtle gradient for a modern touch */
    .login-card-header {
      background: linear-gradient(135deg, #2E86AB, #4ECDC4);
      color: white;
      padding: 1.5rem;
      text-align: center;
    }
    
    .login-card-header h4 {
      font-weight: 600; /* Makes the header text bolder */
      margin-bottom: 0;
    }
    
    .login-card-body {
      padding: 2.5rem; /* Increased padding for better spacing */
    }
    
    .input-group-text {
      background-color: #f1f3f5; /* Light gray background for input icons */
      border-right: none;
    }
    
    .form-control {
      border-left: none;
    }
    
    .form-control:focus {
      border-color: #2E86AB;
      box-shadow: 0 0 0 0.25rem rgba(46, 134, 171, 0.25);
    }
    
    .btn-admin {
      background-color: #2E86AB;
      color: white;
      width: 100%;
      padding: 0.75rem; /* Larger button for better tap targets */
      font-weight: 600;
      transition: background-color 0.3s ease, transform 0.3s ease; /* Smooth hover transition */
      border: none;
    }
    
    .btn-admin:hover {
      background-color: #246a8a;
      transform: translateY(-2px); /* Slight lift on hover */
      color: white;
    }
  </style>
</head>
<body class="bg-light">
  <div class="login-container">
    <div class="login-logo">
      <img src="/images/melbaLogo.png" alt="Melba Community Center Admin" height="250">
    </div>
    
    <div class="card login-card">
      <div class="card-header login-card-header">
        <h4><i class="fas fa-lock me-2"></i> Admin Portal</h4>
      </div>
      
      <div class="card-body login-card-body">
        <% if (typeof error !== 'undefined') { %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            Invalid credentials. Please try again.
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% } %>
        
        <form action="/admin/login" method="POST">
          <div class="mb-3">
            <label for="username" class="form-label">Username</label>
            <div class="input-group">
              <span class="input-group-text"><i class="fas fa-user"></i></span>
              <input type="text" class="form-control" id="username" name="username" required autofocus>
            </div>
          </div>
          
          <div class="mb-4">
            <label for="password" class="form-label">Password</label>
            <div class="input-group">
              <button class="btn btn-outline-secondary" type="button" id="password-toggle">
                <i class="fas fa-eye-slash"></i>
              </button>
              <input type="password" class="form-control" id="password" name="password" required>
            </div>
          </div>
          
          <button type="submit" class="btn btn-admin btn-lg">
            <i class="fas fa-sign-in-alt me-2"></i> Login
          </button>
        </form>
      </div>
    </div>
    
    <div class="text-center mt-3 text-muted small">
      © <%= new Date().getFullYear() %> Melba Community Center Admin
    </div>
  </div>
  <script>
    // Add this code inside your existing <script> block
      document.addEventListener('DOMContentLoaded', function() {
      const passwordInput = document.getElementById('password');
      const passwordToggle = document.getElementById('password-toggle');

      if (passwordToggle) {
              passwordToggle.addEventListener('click', function() {
                  // Get the current type of the input field
                  const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
                  
                  // Toggle the type attribute
                  passwordInput.setAttribute('type', type);
                  
                  // Toggle the icon to match the new state
                  this.querySelector('i').classList.toggle('fa-eye');
                  this.querySelector('i').classList.toggle('fa-eye-slash');
              });
          }
      });
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>